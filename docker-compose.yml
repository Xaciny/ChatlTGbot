services:
  bot:
    build: .
    container_name: chatl-bot
    restart: unless-stopped
    working_dir: /app
    command: python bot.py
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      API_KEY: ${API_KEY}
